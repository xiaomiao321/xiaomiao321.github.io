---
title: 卡尔曼滤波学习笔记
date: 2025-08-27 18:25:00 +0800
categories: [Learning, Algorithm]
tags: [kalman-filter, algorithm]
description: 卡尔曼滤波算法基础学习笔记
toc: true
comments: true
math: true
---

## 入门

### 引入

**滤波**：去掉噪声，使信号更趋于真实值

**卡尔曼滤波**是有多个不确定的结果，经过分析、推理和计算，获得相对准确的结果。

- **多个**是指数据来源可以是模型推理得出，也可以是通过仪器测量获得。
- **不确定**是指由于模型本身是一种近似，或者是测量仪器本身的精度误差，或者测量过程不可避免地引入了噪声，甚至因为所需要的特征无法直接获取，只能间接推导获得。
- **分析、推理和计算**，则指的是卡尔曼滤波算法。
- **相对准确**，指的是经过卡尔曼滤波算法获得的结果，比原有的多个不确定的结果更逼近客观真实值，但依然存在误差。

### 适用系统

**线性高斯系统**

- **线性**
  - 叠加性 —— $f(a+b) = f(a)+f(b)$
  - 齐次性 —— $f(kx) = kf(x)$

- **高斯**——噪声满足正态分布

### 宏观意义

**滤波即加权**

理想状态：信号 x1 + 噪声 x0

卡尔曼滤波：估计值 xX + 观测值 xY

因为估计值和观测值都有一定的误差，所以二者加权能够相对准确，权重取决于二者的方差，方差大的说明不可靠，权重应该给的小一点，反之亦然。

## 进阶

### 状态空间表达式

**基本假设**：

1. 当前时刻状态只和**上一时刻**状态有关。
2. 模型和系统均满足**线性**关系。
3. 引入的噪声符合**高斯**分布。

**状态方程**

$$
x_k = A x_{k-1} + B u_{k-1} + w_k
$$

- $x_k$：k 时刻的真实值，待估计的
- $x_{k-1}$：k-1 时刻的真实值
- $u_{k-1}$：k-1 时刻的控制输入量
- $w_k$：表示过程噪声，且符合均值为 0，协方差矩阵为 Q 的高斯噪声分布

**观测方程**

$$
y_k = C x_k + v_k
$$

- $y_k$：k 时刻的**观测值**
- $v_k$：测量噪声，且符合均值为 0，协方差矩阵为 R 的高斯噪声分布

例如，煮水，$x_k$ 是水的温度，$y_k$ 是温度计显示的水温，则 $y_k = x_k + v_k$

$$
x_k = x_{k-1} + \Delta T + w_k
$$

### 参数分析

#### 噪声

#### 方差

多维方差：协方差矩阵

> [!NOTE]
>
> 本文档为学习笔记摘要，建议参考完整的卡尔曼滤波教材获取更详细的内容。
